class AudioFile {
  int id;
  String name;
  String url;
  String thumbnail;
  AudioFile(
      {required this.id,
      required this.name,
      required this.url,
      required this.thumbnail});
}
