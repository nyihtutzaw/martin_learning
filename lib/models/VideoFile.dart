class VideoFile {
  int id;
  String name;
  String url;
  String thumbnail;
  VideoFile(
      {required this.id,
      required this.name,
      required this.url,
      required this.thumbnail});
}
